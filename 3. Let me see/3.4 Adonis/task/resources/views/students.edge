<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Students</title>
  <style>
    .center {
      margin-left: auto;
      margin-right: auto;
    }

    td {
      padding-left: 10px;
      padding-right: 10px;
    }
  </style>
  @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>
<body>
  @if(students)
    <table class="center">
      <tr>
        <th>Index</th>
        <th>First name</th>
        <th>Last name</th>
      </tr>
      @each(student in students)
        <tr>
          <td><a href="/{{ student.index }}">{{ student.index }}</a></td>
          <td>{{ student.firstName }}</td>
          <td>{{ student.lastName }}</td>
        </tr>
      @endeach
    </table>
    @else
      <p>No students available.</p>
    @end
</body>
</html>
