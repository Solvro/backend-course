<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title> Members table </title>

    @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>

<body>
    <a href="/members"><-Powrót </a>

            @each(error in flashMessages.get('errors')) 
                <p style="color:red">{{error}}</p>
            @endeach

            <form action="/members/create" method="POST">
                {{ csrfField() }}
                <input type="number" value="{{ old('index') || '' }}" placeholder="Index" name="index">
                <input type="text" placeholder="Imię" name="first_name" value="{{ old('first_name') || '' }}">
                <input type="text" placeholder="Nazwisko" name="last_name" value="{{ old('last_name') || '' }}">
                <select name="status" value="{{ old('last_name') || '' }}">
                    <option value="implementation" {{ old('last_name')=='implementation' ?'selected':''}}>implementation
                    </option>
                    <option value="active" {{ old('last_name')=='active' ?'selected':''}}>active</option>
                    <option value="alumni" {{ old('last_name')=='alumni' ?'selected':''}}>alumni</option>
                    <option value="inactive" {{ old('last_name')=='inactive' ?'selected':''}}>inactive</option>
                </select> <button type="submit">Send</button>
            </form>
</body>

</html>